env=prod
browser=chrome
testURL=https://www.zoho.com




undrestood the flow but keep in mind
we run program by 3 ways 
1)actual script
2)testng.xml file
3)and externally like when we use jenkins or run loccally it uses pom.xml file here the surefuleplugin is added and in that we mentioned te .xml file to run 
so always update a name there while running te file 

so in our project we added headless brower and headed and headed is defalut so whenever we run the progrm in eclips we eun headed so if we wnt to run it headeless
we need to do it specifilaly like 2 ways
1)using vm 
-go to .xml file--run as-as configuration--argumets--add the cmd in vm argumets sec:  -DexecutionMode=headless
-and then run so it will run headlessly.
2)using mvn cmmand on cmd 
-go to project location:
cd C:\Users\Dell\eclipse-workspace_TestNG_automation\TestNG_auto_framework
-check the file are presnt is folder cmd for that : dir
mvn clean test                            <---------------run with headed and took .xml file from pom.xml we mentioned there
mvn clean test -DsuiteXmlFile=testng.xml  <---------------here we specified which xml we want to excute steasd of taking from pom.xml we ca specify

but it still run the pom.xml cos we have not metioned "${suiteXmlFile}" this so it willalways take Test_script.xml 
now aded above and run same it worked 

for headless browser i use cmd like
mvn test -DexecutionMode=headless                             <-----single param
mvn test -DexecutionMode=headless -Dbrowser=chrome -Denv=qa   <-----multi param
mvn test -DexecutionMode=headless -Dbrowser=edge -Denv=prod   <------multi but different param


